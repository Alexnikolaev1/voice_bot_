{
  "name": "max-tts-bot",
  "version": "2.0.0",
  "description": "TTS bot for MAX messenger (Yandex SpeechKit, Vercel serverless)",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "check": "node --check api/webhook.js && node --check lib/router.js"
  },
  "dependencies": {
    "@vercel/kv": "^2.0.0",
    "axios": "^1.7.2",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}
